extends layout.pug

block content

  article.portfolio-intro

    //- Replace with your own heading like 'My Portfolio', or 'Hey, Potential Employers', or 'Let's make the web great together!' 
    h1 Let's make the web great together! 

    //- Replace with a description for your portfolio, what this project is, it's purpose, what it's supposed to accomplish, etc. 
    p These are the projects i've created and worked on in the past couple of months, were i've utilized Js and other technologies.


  article.portfolio-project-thumbs
    .projects-box.box.grid

  each i in dataP
    .cell
      a(href=`/projects/${i.id}`)
        img.thumbnail(src=`${i.image_url.thumbnail}` alt=`${i.project_name}`)
        h5= `${i.project_name}`

